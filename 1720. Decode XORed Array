Time: O(n)  Space: O(1)

class Solution {
public:
    vector<int> decode(vector<int>& en, int f) {
        int n=size(en);
        vector<int> res(n+1);
        res[0]=f;
        for(int i=1;i<=n;i++)
            res[i]=res[i-1]^en[i-1];
        return res;
    }
};

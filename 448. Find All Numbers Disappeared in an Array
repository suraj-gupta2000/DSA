class Solution {
public:
    vector<int> findDisappearedNumbers(vector<int>& nums) {
        vector<int> res;int n=size(nums);
        set<int> s;
        for(auto& x: nums)
            s.insert(x);
        int i=1;
        for(auto x: s){
            if(x!=i){
                while(x!=i){
                    res.push_back(i);
                    i++;
                }
            }
            i++;
        }
        while(i<=n){
            res.push_back(i);
            i++;
        }
        return res;
    }
};

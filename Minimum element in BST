Time: O(log n)

int minValue(Node* root) {
    // Code here
    if(!root)
    return -1;
    if(!root->left)
    return root->data;
    Node* curr=root;
    while(curr->left){
        curr=curr->left;
    }
    return curr->data;
}
